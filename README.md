It's an arithmetic progression solver and map plotter?

# IntelliMAP AP Engine
A Flask-based mathematical engine to solve Arithmetic Progression (AP) problems and visualize their trajectories.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the server: `python app.py`
3. Access at `http://127.0.0.1:5000`
